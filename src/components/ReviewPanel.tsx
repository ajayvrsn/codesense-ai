import { motion } from 'motion/react';
import { AlertCircle, Bug, FileText, ShieldAlert, Split, Zap } from 'lucide-react';
import { cn } from '../lib/utils';
import { CodeIssue, ReviewResult } from '../types';

interface ReviewPanelProps { review?: ReviewResult; onApplyRefactor: (issue: CodeIssue) => void; isDebugging?: boolean; }

export function ReviewPanel({ review, onApplyRefactor, isDebugging }: ReviewPanelProps) {
  if (!review) return <div className="flex flex-col items-center justify-center h-full text-slate-500 p-12 text-center"><div className="w-16 h-16 bg-slate-900 rounded-2xl flex items-center justify-center mb-6 border border-border-subtle shadow-xl"><AlertCircle size={32} className="text-slate-700" /></div><p className="text-lg font-semibold text-slate-300 mb-2">No Analysis Yet</p><p className="text-sm text-slate-500 leading-relaxed">Upload a file and run analysis to see AI-powered insights and bug reports.</p></div>;
  return <div className="h-full flex flex-col glass-panel"><div className="p-6 border-b border-border-subtle"><div className="flex items-center justify-between mb-6"><div className="flex flex-col gap-1"><h2 className="text-xl font-bold text-slate-100 tracking-tight">Analysis</h2><span className="text-[10px] font-bold uppercase text-blue-500 tracking-[0.2em]">{isDebugging ? 'Deep Debug Mode' : 'Standard Review'}</span></div><div className={cn('px-4 py-1.5 rounded-full text-sm font-bold shadow-lg', review.overallScore >= 80 ? 'bg-green-500/10 text-green-400 border border-green-500/20 shadow-green-500/5' : review.overallScore >= 60 ? 'bg-yellow-500/10 text-yellow-400 border border-yellow-500/20 shadow-yellow-500/5' : 'bg-red-500/10 text-red-400 border border-red-500/20 shadow-red-500/5')}>Score: {review.overallScore}</div></div><div className="p-4 rounded-xl bg-slate-900/40 border border-border-subtle"><p className="text-sm text-slate-400 leading-relaxed italic">&quot;{review.summary}&quot;</p></div></div><div className="flex-1 overflow-y-auto p-6 space-y-6"><h3 className="text-[10px] font-bold text-slate-500 uppercase tracking-[0.2em]">Issues Found ({review.issues.length})</h3><div className="space-y-4">{review.issues.map((issue, index) => <motion.div initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * 0.05, duration: 0.4, ease: 'easeOut' }} key={index} className="card-issue group"><div className="flex items-start gap-4"><div className={cn('p-2.5 rounded-xl shadow-sm transition-transform group-hover:scale-110 duration-300', issue.type === 'bug' ? 'bg-red-500/10 text-red-400 border border-red-500/20' : issue.type === 'security' ? 'bg-orange-500/10 text-orange-400 border border-orange-500/20' : issue.type === 'performance' ? 'bg-blue-500/10 text-blue-400 border border-blue-500/20' : 'bg-slate-500/10 text-slate-400 border border-slate-500/20')}>{issue.type === 'bug' ? <Bug size={18} /> : issue.type === 'security' ? <ShieldAlert size={18} /> : issue.type === 'performance' ? <Zap size={18} /> : <FileText size={18} />}</div><div className="flex-1 min-w-0"><div className="flex items-center justify-between mb-2"><span className="text-[10px] font-bold uppercase text-slate-500 tracking-wider">Line {issue.line} • {issue.severity}</span><span className={cn('px-2 py-0.5 rounded text-[9px] font-bold uppercase tracking-wider', issue.severity === 'high' ? 'bg-red-500 text-white' : issue.severity === 'medium' ? 'bg-orange-500 text-white' : 'bg-slate-800 text-slate-400')}>{issue.type}</span></div><p className="text-sm text-slate-200 font-semibold mb-1.5 leading-snug">{issue.message}</p><p className="text-xs text-slate-400 mb-4 leading-relaxed">{issue.suggestion}</p>{issue.refactoredCode && <button onClick={() => onApplyRefactor(issue)} className="w-full py-2 bg-blue-500/5 hover:bg-blue-500/10 text-blue-400 rounded-lg text-[11px] font-bold flex items-center justify-center gap-2 transition-all border border-blue-500/20"><Split size={14} />Compare Refactor</button>}</div></div></motion.div>)}</div></div></div>;
}
